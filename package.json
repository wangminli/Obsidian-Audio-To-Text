{
  "name": "obsidian-audio-to-text",
  "version": "0.4.3",
  "private": true,
  "description": "Transcribe Obsidian audio embeds with configurable speech-to-text services.",
  "main": "main.js",
  "scripts": {
    "build": "tsc --noEmit && node esbuild.config.mjs --production",
    "dev": "node esbuild.config.mjs --watch"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.24.2",
    "obsidian": "^1.8.7",
    "typescript": "^5.7.2"
  }
}
